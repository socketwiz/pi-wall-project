/* global describe, it */

import {expect} from 'chai';
import {{ camelCase container }}Reducer from '../{{dashCase container}}';

describe('{{ camelCase container }}Reducer', () => {
    it('returns the initial state', () => {
        expect({{ camelCase container }}Reducer(undefined, {})).to.eql({});
    });
});

