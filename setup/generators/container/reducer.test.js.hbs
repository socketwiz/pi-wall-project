/* global describe, it */

import {expect} from 'chai';
import {{ camelCase name }}Reducer from '../{{dashCase name}}';

describe('{{ camelCase name }}Reducer', () => {
    it('returns the initial state', () => {
        expect({{ camelCase name }}Reducer(undefined, {})).to.eql({});
    });
});

