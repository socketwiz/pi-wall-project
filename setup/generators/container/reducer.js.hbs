/*
 *
 * {{ properCase container }} reducer
 *
 */

import {
    DEFAULT_ACTION
} from '../containers/{{properCase container}}/constants';

const initialState = {};

function {{ camelCase container }}Reducer(state = initialState, action) {
    switch (action.type) {
        case DEFAULT_ACTION:
            return state;
        default:
            return state;
    }
}

export default {{ camelCase container }}Reducer;

