/* global describe, it */

import {expect} from 'chai';
import {
    defaultAction
} from '../{{dashCase container}}';
import {
    DEFAULT_ACTION
} from '../../containers/{{properCase container}}/constants';

describe('{{ properCase container }} actions', () => {
    describe('Default Action', () => {
        it('has a type of DEFAULT_ACTION', () => {
            const expected = {
                type: DEFAULT_ACTION
            };
            expect(defaultAction()).to.eql(expected);
        });
    });
});
