/* global describe, it */

// import {{curly true}}{{properCase container}}{{curly}} from '../index';

import {expect} from 'chai';
// import {{curly true}}mount{{curly}} from 'enzyme';
import React from 'react';

describe('<{{properCase container}} />', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).to.eql(false);
    });
});

